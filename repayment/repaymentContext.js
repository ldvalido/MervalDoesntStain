var Q = require('q');

function calculateRepayment(returnValue, title,year,month, amount) {
 	var q = Q.defer();
 	var operations = {
 		
 	}
	return q.promise;
}

module.exports = {
	calculateRepayment
}